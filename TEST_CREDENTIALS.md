# 🔐 Тестовые учетные данные

## Для локальной разработки

### Dashboard Admin Panel
**URL:** http://localhost:3004/login

**Учетные данные:**
```
Username: admin
Password: admin123dev
```

> **Примечание:** Username может быть любым (например: admin, test, demo), но пароль должен совпадать с `ADMIN_SECRET` в `.env.local`

---

## Как это работает

1. **Логин:** Введите любое имя пользователя и пароль из `.env.local`
2. **Аутентификация:** Пароль проверяется на соответствие `ADMIN_SECRET`
3. **Токен:** После успешного входа создается токен и сохраняется в httpOnly cookie
4. **Сессия:** Токен действует 7 дней

---

## Для production

В production окружении установите надежный `ADMIN_SECRET` в переменные окружения Vercel:

```bash
# Vercel Environment Variables
ADMIN_SECRET=your_very_secure_password_here_minimum_32_characters
NEXT_PUBLIC_API_URL=https://api.outlivion.space
NEXTAUTH_URL=https://dashboard.outlivion.space
NEXTAUTH_SECRET=your_nextauth_secret_minimum_32_characters
```

---

## Безопасность

### ✅ Хорошие практики:
- Используйте длинные пароли (минимум 32 символа)
- Используйте генератор паролей
- Никогда не коммитьте `.env.local` в Git
- Меняйте пароли регулярно

### ❌ НЕ используйте в production:
- `admin123dev`
- `password`
- `123456`
- Любые простые пароли

---

## Рекомендуемые пароли для production

Сгенерируйте надежный пароль:

```bash
# Linux/Mac
openssl rand -base64 32

# Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Online
https://www.random.org/passwords/
```

Пример надежного пароля:
```
ADMIN_SECRET=K9mP2vQ7xR4sT8wY3nB6cF1hJ5lA0zX9
```

---

## Смена пароля

Чтобы сменить пароль администратора:

1. Обновите `ADMIN_SECRET` в `.env.local` (для локальной разработки)
2. Обновите `ADMIN_SECRET` в Vercel Dashboard (для production)
3. Все текущие сессии будут аннулированы
4. Пользователи должны будут войти заново

---

## Будущие улучшения

Планируется добавить:
- [ ] Двухфакторная аутентификация (2FA)
- [ ] Множественные администраторы с разными паролями
- [ ] Ролевая модель (admin, moderator, viewer)
- [ ] История входов
- [ ] Уведомления о входе с нового устройства

---

**Последнее обновление:** Декабрь 2024

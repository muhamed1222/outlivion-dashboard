# Outlivion Dashboard

Админ-панель для управления VPN платформой Outlivion.

## 🚀 Быстрый старт

### Требования
- Node.js 20+
- pnpm (рекомендуется) или npm

### Установка

```bash
# Установить зависимости
pnpm install

# Настроить .env.local
cp .env.example .env.local
# Отредактируйте .env.local со своими настройками
```

### Разработка

```bash
# Запустить в dev режиме
pnpm dev

# Откройте http://localhost:3004
```

### Production

```bash
# Собрать проект
pnpm build

# Запустить production сервер
pnpm start
```

## 📦 Основные команды

| Команда | Описание |
|---------|----------|
| `pnpm dev` | Запуск в dev режиме (порт 3004) |
| `pnpm build` | Сборка для production |
| `pnpm start` | Запуск production сервера |
| `pnpm lint` | ESLint проверка кода |

## 🔧 Переменные окружения

Создайте файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3001
ADMIN_SECRET=your_admin_secret_key
```

## 📊 Функции

### Главная страница
- Общая статистика (пользователи, подписки, доход)
- Графики роста
- Последняя активность

### Users
- Список всех пользователей
- Информация о балансе
- Статус подписки

### Servers
- Мониторинг серверов
- Загрузка серверов
- Количество пользователей на каждом сервере

### Payments
- История всех платежей
- Статусы транзакций
- Фильтрация и поиск

### Subscriptions
- Управление подписками
- Активные/истекшие подписки

## 🛠 Технологии

- **Next.js 14** - React framework (App Router)
- **React 18** - UI библиотека
- **TypeScript** - Язык программирования
- **TailwindCSS** - CSS framework
- **Tremor** - UI компоненты для дашбордов
- **Recharts** - Графики и визуализация данных
- **Axios** - HTTP клиент

## 📊 Структура проекта

```
src/
├── app/
│   ├── layout.tsx           # Root layout с sidebar
│   ├── page.tsx             # Главная (статистика)
│   ├── users/              # Управление пользователями
│   ├── servers/            # Мониторинг серверов
│   ├── payments/           # История платежей
│   └── subscriptions/      # Подписки
├── components/
│   ├── sidebar.tsx         # Боковое меню
│   ├── stats-card.tsx      # Карточка статистики
│   └── charts/             # Компоненты графиков
└── lib/
    └── api.ts              # API клиент
```

## 🔐 Безопасность

- Требуется admin токен для доступа
- Middleware для проверки авторизации
- Защищенные API endpoints

## 🚀 Деплой

### Vercel (рекомендуется)

```bash
vercel deploy
```

Домен: `dashboard.outlivion.space`

## 📄 Лицензия

ISC

---

**Часть Outlivion VPN Platform**


# Outlivion Dashboard

Полнофункциональная админ-панель для управления VPN платформой Outlivion.

## 🚀 Быстрый старт

### Требования
- Node.js 20+
- pnpm (рекомендуется) или npm

### Установка

```bash
# Установить зависимости
pnpm install

# Настроить переменные окружения
cp .env.example .env.local
# Отредактируйте .env.local со своими настройками
```

### Разработка

```bash
# Запустить в dev режиме
pnpm dev

# Откройте http://localhost:3004
```

### Production

```bash
# Собрать проект
pnpm build

# Запустить production сервер
pnpm start
```

## 📦 Основные команды

| Команда | Описание |
|---------|----------|
| `pnpm dev` | Запуск в dev режиме (порт 3004) |
| `pnpm build` | Сборка для production |
| `pnpm start` | Запуск production сервера |
| `pnpm lint` | ESLint проверка кода |
| `pnpm format` | Форматирование кода с Prettier |
| `pnpm format:check` | Проверка форматирования |

## 🔧 Переменные окружения

Создайте файл `.env.local`:

```env
# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Admin Authentication
NEXT_PUBLIC_ADMIN_SECRET=your_admin_secret_key_here

# Optional: NextAuth Configuration
NEXTAUTH_URL=http://localhost:3004
NEXTAUTH_SECRET=your_nextauth_secret_key_here
```

## 📊 Функции

### 🔐 Аутентификация
- ✅ Защищенные роуты с middleware
- ✅ Страница логина
- ✅ Безопасное хранение токенов
- ✅ Автоматический редирект при 401

### 📈 Dashboard (Главная страница)
- ✅ Общая статистика в реальном времени
- ✅ Графики роста пользователей
- ✅ Топ серверов
- ✅ Последняя активность
- ✅ Автообновление каждые 30 секунд

### 👥 Users (Пользователи)
- ✅ Список всех пользователей с пагинацией
- ✅ Поиск по имени, username, Telegram ID
- ✅ Информация о балансе
- ✅ Реферальные коды
- ✅ Real-time данные через SWR

### 🖥️ Servers (Серверы)
- ✅ Мониторинг всех серверов
- ✅ Загрузка и статус в реальном времени
- ✅ Управление серверами (включение/выключение)
- ✅ Количество пользователей на каждом сервере
- ✅ Цветовая индикация нагрузки

### 💳 Payments (Платежи)
- ✅ История всех платежей с пагинацией
- ✅ Поиск по ID платежа/пользователя
- ✅ Статусы транзакций с цветовой индикацией
- ✅ Возможность возврата средств
- ✅ Фильтрация и сортировка

### 🎫 Subscriptions (Подписки)
- ✅ Полноценное управление подписками
- ✅ Статистика по статусам
- ✅ Поиск и фильтрация
- ✅ Отмена и продление подписок
- ✅ Автопродление

### 🎨 UX/UI
- ✅ Toast уведомления для всех действий
- ✅ Error boundaries для обработки ошибок
- ✅ Loading состояния
- ✅ Адаптивный дизайн
- ✅ Современный интерфейс

## 🛠 Технологии

- **Next.js 14** - React framework (App Router)
- **React 18** - UI библиотека
- **TypeScript** - Язык программирования
- **TailwindCSS** - CSS framework
- **Tremor** - UI компоненты для дашбордов
- **Recharts** - Графики и визуализация данных
- **Axios** - HTTP клиент
- **SWR** - Кэширование и синхронизация данных
- **React Hot Toast** - Toast уведомления
- **Zod** - Валидация схем

## 📊 Структура проекта

```
src/
├── app/
│   ├── layout.tsx              # Root layout с sidebar и toaster
│   ├── page.tsx                # Главная (статистика)
│   ├── login/                  # Страница логина
│   ├── error.tsx               # Error boundary
│   ├── loading.tsx             # Loading UI
│   ├── users/                  # Управление пользователями
│   ├── servers/                # Мониторинг серверов
│   ├── payments/               # История платежей
│   └── subscriptions/          # Подписки (полная реализация)
├── components/
│   ├── sidebar.tsx             # Боковое меню с логаутом
│   ├── stats-card.tsx          # Карточка статистики
│   ├── pagination.tsx          # Компонент пагинации
│   └── search-bar.tsx          # Компонент поиска
├── hooks/
│   └── useApi.ts               # SWR хуки для API
├── lib/
│   └── api.ts                  # API клиент с типами
├── types/
│   └── index.ts                # Общие TypeScript типы
└── middleware.ts               # Middleware для авторизации
```

## 🔐 Безопасность

- ✅ Middleware для проверки авторизации
- ✅ Защита всех роутов кроме /login
- ✅ Secure cookies (httpOnly, sameSite, secure в production)
- ✅ Автоматический logout при 401
- ✅ Валидация с Zod
- ✅ TypeScript для типобезопасности

## 🚀 API Integration

### Использование

Приложение подключается к API через `/admin/*` эндпоинты:

```typescript
// Примеры эндпоинтов
GET  /admin/stats                       # Статистика
GET  /admin/users                       # Список пользователей
GET  /admin/users/:id                   # Пользователь по ID
PUT  /admin/users/:id                   # Обновить пользователя
GET  /admin/servers                     # Список серверов
PUT  /admin/servers/:id/toggle          # Включить/выключить сервер
GET  /admin/payments                    # Список платежей
POST /admin/payments/:id/refund         # Возврат средств
GET  /admin/subscriptions               # Список подписок
POST /admin/subscriptions/:id/cancel    # Отменить подписку
POST /admin/subscriptions/:id/renew     # Продлить подписку
```

### Кэширование

Используется SWR для автоматического кэширования и обновления:
- Stats обновляется каждые 30 секунд
- Servers обновляется каждые 10 секунд
- Остальные данные кэшируются до мутации

## 📝 Code Quality

### ESLint
```bash
pnpm lint
```

Настроено:
- Next.js правила
- TypeScript strict правила
- Запрет на использование `any`
- Проверка unused переменных

### Prettier
```bash
pnpm format
```

Настроено:
- Без точек с запятой
- Одинарные кавычки
- 2 пробела отступ
- 100 символов максимум

## 🚀 Деплой

### Vercel (рекомендуется)

```bash
vercel deploy
```

Настроено в `vercel.json`:
- Использует pnpm
- Region: iad1
- Auto deploy на push

Домен: `dashboard.outlivion.space`

### Docker

```bash
# Сборка
docker build -t outlivion-dashboard .

# Запуск
docker run -p 3004:3004 outlivion-dashboard
```

## 🧪 Будущие улучшения

- [ ] Unit тесты (Jest + React Testing Library)
- [ ] E2E тесты (Playwright)
- [ ] Темная тема
- [ ] Экспорт данных в CSV/Excel
- [ ] Breadcrumbs навигация
- [ ] Real-time обновления через WebSocket
- [ ] Продвинутая аналитика и отчеты
- [ ] Управление ролями и правами

## 📄 Лицензия

ISC

---

**Часть Outlivion VPN Platform**

Версия: 2.0.0 (Полностью переработано)

# 🚀 Production Setup для Outlivion Dashboard

## 1. Установка переменных окружения в Vercel

### Способ A: Через Vercel Dashboard (веб-интерфейс)

1. Откройте https://vercel.com/dashboard
2. Выберите проект `outlivion-dashboard`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте следующие переменные:

```env
# API Configuration (ОБЯЗАТЕЛЬНО!)
NEXT_PUBLIC_API_URL=https://api.outlivion.space

# Admin Password (ОБЯЗАТЕЛЬНО!)
# Сгенерируйте надежный пароль (минимум 32 символа)
ADMIN_SECRET=ваш_супер_надежный_пароль_минимум_32_символа

# NextAuth (опционально, для будущего)
NEXTAUTH_URL=https://dashboard.outlivion.space
NEXTAUTH_SECRET=другой_надежный_ключ_минимум_32_символа

# Environment
NODE_ENV=production
```

5. Нажмите **Save**
6. Redeploy проект

### Способ B: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Добавьте переменные
vercel env add ADMIN_SECRET
# Введите надежный пароль

vercel env add NEXT_PUBLIC_API_URL
# Введите: https://api.outlivion.space

# Redeploy
vercel --prod
```

---

## 2. Генерация надежного пароля

### Используйте один из методов:

```bash
# Метод 1: OpenSSL (Mac/Linux)
openssl rand -base64 32

# Метод 2: Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Метод 3: Online генератор
# https://www.random.org/passwords/
# Настройки: 32+ символов, все типы символов
```

### Пример надежного пароля:
```
ADMIN_SECRET=K9mP2vQ7xR4sT8wY3nB6cF1hJ5lA0zX9eD4gH8jM2nP5r
```

---

## 3. Вход после деплоя

После установки переменных окружения:

1. Откройте `https://dashboard.outlivion.space/login`
2. Введите:
   - **Username:** admin (или любое имя)
   - **Password:** Ваш ADMIN_SECRET
3. Нажмите **Войти**

---

## ⚠️ Важные заметки

### О текущей системе аутентификации:

**Плюсы:**
- ✅ Простая и быстрая
- ✅ Безопасные cookies (httpOnly, secure)
- ✅ Middleware защита

**Минусы:**
- ❌ Только один "аккаунт" (один пароль)
- ❌ Username не проверяется
- ❌ Нельзя создать несколько админов
- ❌ Нет истории входов

---

## 💡 Улучшенная система аккаунтов (для будущего)

Если нужна полноценная система с несколькими админами:

### Вариант 1: Простой - несколько паролей

Создайте JSON с учетными данными:

```typescript
// src/lib/admins.ts
const admins = {
  'admin': 'password_hash_1',
  'moderator': 'password_hash_2',
}
```

### Вариант 2: База данных (рекомендуется)

Создайте таблицу `admins` в Neon PostgreSQL:

```sql
CREATE TABLE admins (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  username VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  role VARCHAR(20) DEFAULT 'admin',
  created_at TIMESTAMP DEFAULT NOW(),
  last_login TIMESTAMP
);

-- Создайте первого админа
INSERT INTO admins (username, email, password_hash, role)
VALUES ('admin', 'admin@outlivion.space', 'bcrypt_hash', 'super_admin');
```

### Вариант 3: NextAuth.js (professional)

```bash
# Установить NextAuth
pnpm add next-auth@beta

# Настроить провайдеры
# - Email/Password
# - Google OAuth
# - GitHub OAuth
```

---

## 🎯 Рекомендуемый подход для Outlivion

### Сейчас (Phase 1):
✅ Используйте ADMIN_SECRET (текущая система)
- Быстро
- Достаточно для старта
- Безопасно для одного админа

### Ближайшее будущее (Phase 2):
🔜 Добавьте 2FA к текущей системе
- QR-код с Google Authenticator
- TOTP верификация
- Backup коды

### Долгосрочно (Phase 3):
🔮 Полноценная система
- Таблица admins в базе данных
- Роли (super_admin, admin, moderator, viewer)
- История входов
- Управление админами через UI

---

## 📝 Quick Start для Production

### Минимум для запуска:

```bash
# 1. Откройте Vercel Dashboard
https://vercel.com

# 2. Добавьте переменные
Settings → Environment Variables → Add

Name:  ADMIN_SECRET
Value: <ваш_сгенерированный_пароль>

Name:  NEXT_PUBLIC_API_URL  
Value: https://api.outlivion.space

# 3. Redeploy
Deployments → ... → Redeploy
```

### После деплоя:
```
URL:      https://dashboard.outlivion.space/login
Username: admin
Password: <ваш ADMIN_SECRET>
```

---

## 🔒 Безопасность в Production

### ОБЯЗАТЕЛЬНО:
- ✅ Используйте надежный пароль (32+ символов)
- ✅ Не делитесь ADMIN_SECRET
- ✅ Меняйте пароль регулярно
- ✅ Используйте HTTPS (автоматически в Vercel)

### РЕКОМЕНДУЕТСЯ:
- 🟡 Добавьте 2FA (следующий приоритет)
- 🟡 Настройте уведомления о входах
- 🟡 Логируйте все действия администратора

---

## 🆘 Если забыли пароль

1. Откройте Vercel Dashboard
2. Settings → Environment Variables
3. Найдите `ADMIN_SECRET`
4. Нажмите **Edit** или **Remove** и создайте новый
5. Redeploy проект
6. Все сессии будут сброшены
7. Войдите с новым паролем

---

## 📞 Поддержка

Если нужна помощь:
1. Проверьте логи в Vercel Dashboard
2. Проверьте переменные окружения
3. Убедитесь, что API работает на api.outlivion.space

---

**Готово! Следуйте инструкциям выше для production деплоя!** 🚀

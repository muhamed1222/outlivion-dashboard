# 🔧 Устранение проблем Dashboard

## 🔐 Проблемы с авторизацией

### ❌ "Не получается авторизоваться"

#### Проверка 1: Правильные учетные данные

**Для локальной разработки:**
```
URL:      http://localhost:3004/login
Username: admin (или любое имя)
Password: admin123dev
```

**⚠️ Важно:** Username может быть ЛЮБЫМ, проверяется только пароль!

---

#### Проверка 2: Environment variables

```bash
# Проверьте .env.local
cat .env.local | grep ADMIN_SECRET

# Должно быть:
ADMIN_SECRET=admin123dev
```

**Если файла нет:**
```bash
cat > .env.local << 'EOF'
NEXT_PUBLIC_API_URL=http://localhost:3001
ADMIN_SECRET=admin123dev
NODE_ENV=development
EOF
```

---

#### Проверка 3: Dev сервер запущен

```bash
# Проверьте что dashboard работает
lsof -ti:3004

# Если нет процесса, запустите:
pnpm dev
```

---

#### Проверка 4: Browser Console

1. Откройте DevTools (F12 или Cmd+Option+I)
2. Перейдите в **Console** tab
3. Попробуйте войти
4. Посмотрите на ошибки

**Типичные ошибки:**

**A. Network error:**
```
Failed to fetch
POST http://localhost:3004/api/login net::ERR_CONNECTION_REFUSED
```
**Решение:** Перезапустите dashboard

**B. 500 Internal Server Error:**
```
{error: "Server configuration error"}
```
**Решение:** ADMIN_SECRET не установлен в .env.local

**C. 401 Unauthorized:**
```
{error: "Неверный пароль"}
```
**Решение:** Проверьте пароль (должен быть admin123dev)

**D. CORS error:**
```
Access to fetch blocked by CORS policy
```
**Решение:** API не запущен или неправильный URL

---

#### Проверка 5: Network tab

1. DevTools → **Network** tab
2. Попробуйте войти
3. Найдите запрос `POST /api/login`
4. Посмотрите:
   - Status code (должен быть 200)
   - Response (должен быть {success: true})
   - Request payload (проверьте password)

---

### 🔧 Быстрые решения:

#### Решение 1: Полная перезагрузка
```bash
# 1. Убить все процессы
lsof -ti:3004 | xargs kill -9

# 2. Очистить кэш Next.js
rm -rf .next

# 3. Перезапустить
pnpm dev

# 4. Hard reload в браузере
Cmd+Shift+R (Mac) или Ctrl+Shift+R (Windows)
```

#### Решение 2: Проверить API route
```bash
# Тест API route напрямую
curl -X POST http://localhost:3004/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123dev"}'

# Ожидаемый ответ:
# {"success":true,"message":"Успешный вход"}
```

#### Решение 3: Проверить cookies
1. DevTools → Application tab
2. Storage → Cookies → http://localhost:3004
3. После логина должна появиться cookie `admin_token`
4. Если нет - проблема с сервером

---

## 🌐 Проблемы с API

### ❌ "Failed to load resource: 404"

```
GET http://localhost:3001/admin/stats 404 (Not Found)
```

**Причина:** API не имеет admin routes

**Решение:**
```bash
# 1. Убедитесь что новые admin routes закоммичены
cd /path/to/outlivion-api
git status

# 2. Перезапустите API
lsof -ti:3001 | xargs kill -9
npm run dev

# 3. Проверьте что routes загрузились
curl http://localhost:3001/health
```

---

### ❌ "ERR_CONNECTION_REFUSED"

**Причина:** API не запущен

**Решение:**
```bash
cd /path/to/outlivion-api
npm run dev
```

---

### ❌ "CORS policy"

**Причина:** CORS не настроен для вашего origin

**Решение:** Проверьте что в API `src/index.ts` есть:
```typescript
'http://localhost:3004'  // в allowedOrigins
```

---

## 🎨 UI проблемы

### ❌ "Белый экран" или "Ошибка 500"

**Проверьте Console:**
```
Error: useTheme must be used within ThemeProvider
```

**Решение:** Уже исправлено, но если видите - перезагрузите страницу

---

### ❌ "Моковые данные не показываются"

**Это нормально!** Моковые данные удалены.

**Что показывается:**
- Реальные данные из API
- Или "Нет данных" если API пустой
- Или placeholders для не реализованных endpoints

---

## 📊 Sentry проблемы

### ❌ "Sentry не отправляет ошибки"

**Проверка:**
```bash
# 1. Проверьте что DSN установлен
cat .env.sentry-build-plugin

# 2. Проверьте тестовую страницу
http://localhost:3004/sentry-example-page

# 3. Нажмите кнопки генерации ошибок

# 4. Проверьте в Sentry Dashboard
https://sentry.io
```

---

## 🆘 Последнее средство

Если ничего не помогает:

```bash
# 1. Полная очистка
cd outlivion-dashboard
rm -rf node_modules .next pnpm-lock.yaml
git clean -fdx  # ОСТОРОЖНО! Удалит все неотслеживаемые файлы

# 2. Переустановка
pnpm install

# 3. Запуск
pnpm dev

# 4. Hard reload в браузере
Cmd+Shift+R
```

---

## 🎯 Диагностика прямо сейчас:

Скажите мне:
1. **Какую ошибку вы видите?** (текст ошибки)
2. **Где видите?** (на странице, в console, в network tab)
3. **Что происходит?** (кнопка не работает, редирект, белый экран)

Я помогу решить конкретную проблему! 😊
